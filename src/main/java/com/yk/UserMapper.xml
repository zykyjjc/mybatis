<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.yk.GetInfo">
    <!--<select id="getUser" parameterType="int" resultType="com.yk.User">-->
       <!--select id,name,age from black where id=#{id}-->
    <!--</select>-->
    <select id="getUserList" resultType="com.yk.User">
        select * from black

    </select>
    <!--<insert id="insertUser" parameterType="com.yk.User" useGeneratedKeys="true" keyProperty="id">-->
        <!--insert  into black(name, age) values(#{name},#{age})-->
    <!--</insert>-->
    <!--<update id="updateUser" parameterType="com.yk.User">-->
        <!--update black set name=#{name},age=#{age} where id=#{id}-->
    <!--</update>-->
    <!--<delete id="deleteUser" parameterType="int">-->
        <!--delete from black where id=#{id}-->
    <!--</delete>-->
</mapper>
